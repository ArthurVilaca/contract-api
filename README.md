### Contract API

### how to run

    to run:
    `npm install`
    `npm run seed`
    `npm start`

### unit test

Used supertest to create an example for a test on an API.

    to run:
    `npm install`
    `npm run seed`
    `npm test`